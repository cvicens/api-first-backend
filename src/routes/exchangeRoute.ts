/* import { Router } from "express";
import { getExchange } from "../controllers/exchange";
import { asyncHandler } from "../lib/asyncHandler";
export const exchangeGet  = Router().use("/", asyncHandler(getExchange, "getExchange"));
 */